import 'package:flutter/material.dart';

class Practical2 extends StatelessWidget {
  const Practical2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("practical 2")),
      body: ListView(
        padding: EdgeInsets.all(16),
        children: [
          Center(
            child: Text(
              "this is simple text",
              style: TextStyle(fontWeight: FontWeight.bold, fontSize: 20),
            ),
          ),
          SizedBox(height: 10),
          Image.asset('assets/images/group.jpg', height: 100, width: 100),
          SizedBox(height: 10),
          Container(
            color: Colors.blue,
            child: Text("I am inside container"),
            height: 300,
            width: 200,
          ),
          SizedBox(height: 10),
          ElevatedButton(onPressed: () {}, child: Text("elevated button")),
          SizedBox(height: 10),
          OutlinedButton(onPressed: () {}, child: Text("outlineButton")),
          SizedBox(height: 10),
          TextButton(onPressed: () {}, child: Text("textbutton")),
          SizedBox(height: 10),
          ListTile(
            leading: Icon(Icons.person),
            title: Text("any title"),
            subtitle: Text("any subtitile"),
            trailing: Icon(Icons.add),
          ),
        ],
      ),
    );
  }
}

